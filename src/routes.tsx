
import { Routes } from 'react-router-dom';
import { WrapperRoutes } from './MainRoutes';


const MainRoutes = () => {
  
  return (
    <Routes>
        <WrapperRoutes />  
    </Routes>
  );
};

export default MainRoutes;